<div class="d-none">
    <!-- Contacts Message -->
    <span id="dataContactMessageTitleSendSuccess">{{ translating('contact-message-title-send-success') }}</span>
    <span id="dataContactMessageTitleSendError">{{ translating('contact-message-title-send-error') }}</span>
    <span id="dataContactMessageDescriptionSendSuccess">{{ translating('contact-message-description-send-success') }}</span>
    <span id="dataContactMessageDescriptionSendError">{{ translating('contact-message-description-send-error') }}</span>

    <!-- Post Wishlist Action -->
    <span id="dataPostWishlistActionTitleError">{{ translating('post-wishlist-title-error') }}</span>
    <span id="dataPostWishlistActionDescriptionError">{{ translating('post-wishlist-description-error') }}</span>

    <!-- User Wishlist Action -->
    <span id="dataUserWishlistActionTitleError">{{ translating('user-wishlist-title-error') }}</span>
    <span id="dataUserWishlistActionDescriptionError">{{ translating('user-wishlist-description-error') }}</span>

    <!-- Searches Wishlist Action -->
    <span id="dataSearchesWishlistActionTitleError">{{ translating('searches-wishlist-title-error') }}</span>
    <span id="dataSearchesWishlistActionDescriptionError">{{ translating('searches-wishlist-description-error') }}</span>

    <!-- Send Message To Post Author -->
    <span id="dataMessageSendToPostAuthorTitleSuccess">{{ translating('message-send-to-post-author-title-success') }}</span>
    <span id="dataMessageSendToPostAuthorDescriptionSuccess">{{ translating('message-send-to-post-author-description-success') }}</span>
    <span id="dataMessageSendToPostAuthorTitleError">{{ translating('message-send-to-post-author-title-error') }}</span>
    <span id="dataMessageSendToPostAuthorDescriptionError">{{ translating('message-send-to-post-author-description-error') }}</span>

    <!-- Send Report To Admin -->
    <span id="dataSendReportToAdminTitleSuccess">{{ translating('send-report-to-admin-title-success') }}</span>
    <span id="dataSendReportToAdminDescriptionSuccess">{{ translating('send-report-to-admin-description-success') }}</span>
    <span id="dataSendReportToAdminTitleError">{{ translating('send-report-to-admin-title-error') }}</span>
    <span id="dataSendReportToAdminDescriptionError">{{ translating('send-report-to-admin-description-error') }}</span>

    <!-- Update Post -->
    <span id="dataUpdatePostTitleSuccess">{{ translating('update-post-title-success') }}</span>
    <span id="dataUpdatePostDescriptionSuccess">{{ translating('update-post-description-success') }}</span>
    <span id="dataUpdatePostTitle3DaysLimit">{{ translating('update-post-title-3-day') }}</span>
    <span id="dataUpdatePostDescription3DaysLimit">{{ translating('update-post-description-3-day') }}</span>
    <span id="dataUpdatePostTitleError">{{ translating('update-post-title-error') }}</span>
    <span id="dataUpdatePostDescriptionError">{{ translating('update-post-description-error') }}</span>

    <!-- Delete Post -->
    <span id="dataDeletePostTitleSuccess">{{ translating('delete-post-title-success') }}</span>
    <span id="dataDeletePostDescriptionSuccess">{{ translating('delete-post-description-success') }}</span>
    <span id="dataDeletePostTitleError">{{ translating('delete-post-title-error') }}</span>
    <span id="dataDeletePostDescriptionError">{{ translating('delete-post-description-error') }}</span>

    <!-- Delete Message -->
    <span id="dataDeleteMessageTitleSuccess">{{ translating('delete-message-title-success') }}</span>
    <span id="dataDeleteMessageDescriptionSuccess">{{ translating('delete-message-description-success') }}</span>
    <span id="dataDeleteMessageTitleError">{{ translating('delete-message-title-error') }}</span>
    <span id="dataDeleteMessageDescriptionError">{{ translating('delete-message-description-error') }}</span>

    <!-- Update Message -->
    <span id="dataUpdateMessageTitleSuccess">{{ translating('update-message-title-success') }}</span>
    <span id="dataUpdateMessageDescriptionSuccess">{{ translating('update-message-description-success') }}</span>
    <span id="dataUpdateMessageTitleError">{{ translating('update-message-title-error') }}</span>
    <span id="dataUpdateMessageDescriptionError">{{ translating('update-message-description-error') }}</span>

    <!-- Post Make Passive -->
    <span id="dataPostMakePassiveTitleSuccess">{{ translating('post-make-passive-title-success') }}</span>
    <span id="dataPostMakePassiveDescriptionSuccess">{{ translating('post-make-passive-description-success') }}</span>
    <span id="dataPostMakePassiveTitleError">{{ translating('post-make-passive-title-error') }}</span>
    <span id="dataPostMakePassiveDescriptionError">{{ translating('post-make-passive-description-error') }}</span>

    <!-- Post Make Active -->
    <span id="dataPostMakeActiveTitleSuccess">{{ translating('post-make-active-title-success') }}</span>
    <span id="dataPostMakeActiveDescriptionSuccess">{{ translating('post-make-active-description-success') }}</span>
    <span id="dataPostMakeActiveTitleError">{{ translating('post-make-active-title-error') }}</span>
    <span id="dataPostMakeActiveDescriptionError">{{ translating('post-make-active-description-error') }}</span>

    <!-- Change account phone numbers -->
    <span id="dataAccountPhoneNumbersTitleSuccess">{{ translating('change-account-phone-numbers-title-success') }}</span>
    <span id="dataAccountPhoneNumbersDescriptionSuccess">{{ translating('change-account-phone-numbers-description-success') }}</span>
    <span id="dataAccountPhoneNumbersTitleError">{{ translating('change-account-phone-numbers-title-error') }}</span>
    <span id="dataAccountPhoneNumbersDescriptionError">{{ translating('change-account-phone-numbers-description-error') }}</span>

    <!-- Change account profile datas -->
    <span id="dataAccountProfileDatasTitleSuccess">{{ translating('change-account-profile-datas-title-success') }}</span>
    <span id="dataAccountProfileDatasDescriptionSuccess">{{ translating('change-account-profile-datas-description-success') }}</span>
    <span id="dataAccountProfileDatasTitleError">{{ translating('change-account-profile-datas-title-error') }}</span>
    <span id="dataAccountProfileDatasDescriptionError">{{ translating('change-account-profile-datas-description-error') }}</span>

    <!-- Write review -->
    <span id="dataWriteReviewTitleSendSuccess">{{ translating('write-review-title-success') }}</span>
    <span id="dataWriteReviewDescriptionSendSuccess">{{ translating('write-review-description-success') }}</span>
    <span id="dataWriteReviewTitleSendError">{{ translating('write-review-title-error') }}</span>
    <span id="dataWriteReviewDescriptionSendError">{{ translating('write-review-description-error') }}</span>

    <!-- Delete blocked user -->
    <span id="dataDeleteBlockedUsertTitleSuccess">{{ translating('delete-blocked-user-title-success') }}</span>
    <span id="dataDeleteBlockedUsertDescriptionSuccess">{{ translating('delete-blocked-user-description-success') }}</span>
    <span id="dataDeleteBlockedUsertTitleError">{{ translating('delete-blocked-user-title-error') }}</span>
    <span id="dataDeleteBlockedUsertDescriptionError">{{ translating('delete-blocked-user-description-error') }}</span>

    <!-- Send Message -->
    <span id="dataSendMessageNow">{{ translating('data-send-message-now') }}</span>

    <!-- Create Post -->
    <span id="categorySelected">{{ translating('category-selected') }}</span>
    <span id="uploadImage">{{ translating('upload') }}</span>
    <span id="deleteImage">{{ translating('delete-image') }}</span>
    <span id="changeImage">{{ translating('change-image') }}</span>

    <!-- Post Change Priorities -->
    <span id="postMakedTopSuccessTitle">{{ translating('post-maked-top-success-title') }}</span>
    <span id="postMakedTopSuccessDescription">{{ translating('post-maked-top-success-description') }}</span>
    <span id="postMakedTopBalanceProblemTitle">{{ translating('post-maked-top-balance-problem-title') }}</span>
    <span id="postMakedTopBalanceProblemDescription">{{ translating('post-maked-top-balance-problem-description') }}</span>
    <span id="postMakedTopErrorTitle">{{ translating('post-maked-top-eror-title') }}</span>
    <span id="postMakedTopErrorDescription">{{ translating('post-maked-top-eror-description') }}</span>

    <!-- Post Change Priorities -->
    <span id="postMakedHurrySuccessTitle">{{ translating('post-maked-hurry-success-title') }}</span>
    <span id="postMakedHurrySuccessDescription">{{ translating('post-maked-hurry-success-description') }}</span>
    <span id="postMakedHurryBalanceProblemTitle">{{ translating('post-maked-hurry-balance-problem-title') }}</span>
    <span id="postMakedHurryBalanceProblemDescription">{{ translating('post-maked-hurry-balance-problem-description') }}</span>
    <span id="postMakedHurryErrorTitle">{{ translating('post-maked-hurry-eror-title') }}</span>
    <span id="postMakedHurryErrorDescription">{{ translating('post-maked-hurry-eror-description') }}</span>

    <!-- Moderation Posts Alert -->
    <span id="moderationPostTitle">{{ translating('moderation-posts-title') }}</span>
    <span id="moderationPostDescription">{{ translating('moderation-posts-description') }}</span>
    
    <!-- Filtering -->
    <span id="filterMaxiumPriceValue">{{ $max_price_value }}</span>
</div>
